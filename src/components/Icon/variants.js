import { SearchAlt, Menu } from 'styled-icons/boxicons-regular';
import { Close } from '@styled-icons/evaicons-solid';

const options = {
  search: SearchAlt,
  menu: Menu,
  close: Close,
};

export default options;
